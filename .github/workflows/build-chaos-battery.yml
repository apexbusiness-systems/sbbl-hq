name: Build Chaos Battery

on:
  pull_request:
    paths:
      - 'src/lib/api/**'
      - 'src/lib/supabase/**'
      - 'src/pages/**'
      - 'src/contexts/**'
      - 'src/test/**'
      - 'e2e/**'
      - 'playwright-fixture.ts'
      - 'playwright.config.ts'
      - 'package.json'
      - '.github/workflows/build-chaos-battery.yml'
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: build-chaos-${{ github.ref }}
  cancel-in-progress: true

jobs:
  chaos:
    name: Full Build Chaos Battery
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL || 'https://test-project-ref.supabase.co' }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY || 'test-anon-key' }}
      VITE_APP_NAME: SBBL HQ
      VITE_DEFAULT_LEAGUE: SBBL

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Unit chaos battery
        run: npx vitest run src/test/apifetch-401-retry.test.ts src/test/stream-chaos-battery.test.ts src/test/ops-chaos-battery.test.ts src/test/runtime-config.test.ts src/test/ops-auth-gating.test.tsx

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium webkit

      - name: E2E chaos battery
        run: npx playwright test --project=chromium e2e/build-chaos-battery.spec.ts e2e/ops-auth-ingest-harmony.spec.ts e2e/critical-paths.spec.ts
