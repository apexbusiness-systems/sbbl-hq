### ARTIFACT: Implementation

```diff
// src/components/LiveStreamPlayer.tsx

-  const isNativeUI = window.innerWidth <= 768;
-
   return (
     <div 
       ref={containerRef}
       className="relative w-full aspect-video bg-black rounded-lg overflow-hidden group"
     >
       {/* 1. Video Layer */}
       <div className="absolute inset-0 z-0">
-        <WhepPlayer streamUrl={streamUrl} autoPlay={true} muted={isMuted} />
+        <WhepPlayer streamUrl={streamUrl} autoPlay={true} muted={isMuted} volume={volume} />
       </div>

-      {!isNativeUI && (
+      {/* Volume Slider - always present */}
+      <input
+        type="range"
+        min="0"
+        max="1"
+        step="0.05"
+        value={isMuted ? 0 : volume}
+        onChange={handleVolumeChange}
+        className="w-20"
+        aria-label="Volume"
+      />
```

```diff
// src/components/WhepPlayer.tsx

-  muted: boolean;
+  muted?: boolean;
+  volume?: number;

-    if (videoRef.current && muted !== undefined) {
-      videoRef.current.muted = muted;
+    if (videoRef.current) {
+      if (muted !== undefined) videoRef.current.muted = muted;
+      if (volume !== undefined) videoRef.current.volume = volume;
     }
-  }, [muted]);
+  }, [muted, volume]);
```

```diff
// src/pages/Ops.tsx

-    onSuccess: async (result) => {
-      if (result.ok && result.data) {
-        setIngestJob({
-          jobId: result.data.id,
-          state: result.data.status,
-        });
-      }
+    onSuccess: async (data) => {
+      setIngestJob(data as { jobId: string; state: string });
```
