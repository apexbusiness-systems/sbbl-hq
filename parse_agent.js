const { execSync } = require('child_process');

async function askVision() {
    // Just a quick tool to interact with anthropic / vision via my own agent environment if available,
    // or we can write a small local python script to hit OpenAI/Anthropic since Tesseract isn't working perfectly on these fancy images.
}
